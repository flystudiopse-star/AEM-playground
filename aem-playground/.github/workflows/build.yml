name: Build AEM Project

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temulus'
        cache: 'maven'
        
    - name: Build with Maven
      run: mvn clean install
      
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: aem-packages
        path: |
          **/target/*.zip
          **/target/*.jar